# Multithreaded Process Manager Simulator

This project implements a simulated Operating System process manager in C using POSIX threads and synchronization mechanisms. It manages processes through PCB structures, handles process creation, termination, waiting, and maintains a synchronized process table with concurrent thread operations.
